function addOne(number) {
  number = number + 1;
  return number;
};

function doubleNum(num) {
  num = num * 2;
  return num;
};

function isNegitive(x) {
  if (x < 0) {
    return true;
  }
};

function isEven(y) {
  if (y / 2 = % 0 ) {
    return true;
  }
};
